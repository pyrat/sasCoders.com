http://hollow-meadow-93.heroku.com
heroku db:reset --app hollow-meadow-93
heroku rake db:migrate

What other actions need to be done?
jobs admin(manage) page/ jobs edit page

add a table to the db--> user has a job posting, job posting belongs to a user
  job_posting_user table? or jobPosting_user?
  and then when a job is added we need to user.job_posting << @job  (?)

+add company web site to user

Recruiter display page

 
 <p>Days To Run: <%= form.select(:days_to_run, %w{15 30}) %>